# Xcode
*.xccheckout
*.moved-aside
xcuserdata/
*.xcuserdata
*.xcuserstate
*.xcscmblueprint
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
*.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings
build/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# CocoaPods (not used but just in case)
Pods/
Podfile.lock

# SPM
.build/
.swiftpm/

# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Env
.env

# Claude
.claude/settings.local.json

# Xcode export/archive output
Flowe */
*.xcarchive

# Stray root-level copies of the Figma export (the tracked one is Flowe/Pilates app/)
/Pilates app/
/Pilates app-2/

# CloudKit deploy scratch (transient schema exports — the tracked source of truth is
# CloudKit-dev-schema.ckdb; everything below is a throwaway dump/diff)
dev-live.ckdb
dev-verify.ckdb
CloudKit-full-schema*.ckdb
CloudKit-missing-schema.ckdb
ck-wipe-dev.py

# project.pbxproj backups from hand-editing (xcodegen not installed)
*.pbxproj.bak-*

# Local scratch artifacts (screenshots, API dumps) kept out of the repo
PHOTO-*.jpg
appstore-mockup*.png
openapi.json

# Sensitive / third-party: never commit
# - PII export from the live DB; - Apple's copyrighted review guidelines PDF
database-export-logs-*.json
AppStore-Review-Guidelines.pdf
